1. The natural numbers include zero for us.

2. Is g_delta still increasing at x = 0?

3. Be careful about "isomorphic" in the context of cones. If two
   symmetric cones are isomorphic, they're probably Jordan-isomorphic,
   but in general that's not true. When we say e.g. "up to
   isomorphism" we probably don't mean up to arbitrary cone
   isomorphism.

4. Mention that the Lypaunov rank of \Rnplus is strictly minimal? We
   use this in a few places.

5. Explain the computational motivation for Lypaunov rank. It greatly
   speeds up isomorphism computations.

6. Add a verify-db script to ensure that the local database is suitable
   for running the test suite:

   a) it should have enough dimensions, lorentz ranks, and partitions
      for the theorems that need those things
   b) no two cones should have the same hash
   c) the trivial cases should be in there

7. Partitions should suffice for Ln + Lm, too?
